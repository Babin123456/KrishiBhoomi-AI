# NGINX configuration for routing local reverse proxy traffic to docker services
# Maps /api/v1 routes to Fastapi backend:8000 and standard routing to frontend:3000
# Save under docker directory for environment configurations
